// Hardcoded for production - Constants.expoConfig?.extra may be undefined in Release builds
export const API_URL = 'https://api.homecast.cloud';
export const WS_URL = 'wss://api.homecast.cloud';
export const GRAPHQL_URL = `${API_URL}/`;
export const WS_WEB_URL = `${WS_URL}/ws/web`;
