# Database URL (SQLite for local, PostgreSQL for production)
DATABASE_URL=sqlite:///./homekit_mcp.db

# JWT Configuration
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=168

# Server Configuration
PORT=8080

# CORS Origins (comma-separated)
ALLOWED_CORS_ORIGINS=http://localhost:3000,https://homekitmcp.com,https://www.homekitmcp.com
