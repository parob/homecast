<?xml version="1.0" encoding="utf-8"?>
<!--
    Adaptive-icon background. This used to be the stock Android Studio green grid
    (#3DDC84) left over from `tauri android init`, unreferenced because the
    adaptive icon pointed at a flat @color instead.

    It is now the real brand gradient, so the launcher icon matches every other
    surface. That also retires the #3478F6 the flat colour was using, which had
    drifted from the #3B82F6 the rest of the product ships.

    Generated from brand/params.json — do not hand-edit; run `cd brand && npm run build`.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z">
        <aapt:attr name="android:fillColor">
            <gradient
                android:type="linear"
                android:startX="0"
                android:startY="0"
                android:endX="108"
                android:endY="108"
                android:startColor="#3B82F6"
                android:endColor="#2563EB" />
        </aapt:attr>
    </path>
</vector>
